{
  "name": "writespace",
  "version": "1.0.0",
  "description": "Writing Platform Project",
  "main": "server.js",
  "scripts": {
    "client": "pnpm start --prefix frontend",
    "build": "pnpm nx run-many --target=build --parallel --maxParallel=100",
    "serve": "pnpm nx run-many --target=start --parallel --maxParallel=100",
    "dev": "cd apps/frontend && pnpm dev"
  },
  "author": "Snir Azran",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "colors": "^1.4.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.8.4",
    "webpack": "^5.75.0"
  },
  "devDependencies": {
    "@nrwl/nx-cloud": "latest",
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20",
    "nx": "15.9.2"
  }
}
